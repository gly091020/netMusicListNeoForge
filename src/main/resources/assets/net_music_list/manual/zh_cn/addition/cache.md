<!-- entry.title:缓存功能 -->
<!-- entry.button:all_config -->
<!-- entry.img:manual/addition/cache_config.png,有关配置 -->
# 音乐缓存功能

此功能可以在音乐播放时将其下载到本地。（仅支持网易云音乐）

在启用后（默认禁用），播放音乐时会进行下载，路径：

```游戏文件夹/config/netMusicListCache```

如果启用了**全局缓存**，则是：

```C:\Users\当前用户名\netMusicListCache```